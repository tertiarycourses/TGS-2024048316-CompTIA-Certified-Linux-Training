# Local environment (courseware Drive link etc.) — never pushed
.env

# OS / editor cruft
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Office temp lock files
~$*

# Playwright MCP
.playwright-mcp/

# Local reference material (large vendor deck — not redistributed)
/reference/

# WSQ assessment materials (confidential — never pushed)
/assessment/
/assessment\ /

# Assessment BUILDERS embed the exam questions and model answers — confidential,
# never pushed (the generic toolchain lives in tertiarycourses/wsqskills)
.claude/skills/wsq-assessment/build_assessment.py
.claude/skills/wsq-assessment/build_wsq_assessment.py

# Skill reference IMPLEMENTATIONS are toolchain code, not derived-from source material
!.claude/skills/**/reference/
!.claude/skills/**/reference/**
